# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
env/
venv/
ENV/

# Data and Models
data_download/
*.zip
# We keep scream_models/ because it contains the trained weights
# But we track the binaries with LFS

# IDEs
.vscode/
.idea/

# Streamlit
# We keep config.toml to preserve the localhost server address fix
# .streamlit/config.toml
